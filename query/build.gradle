dependencies {
    api project(':entity-store')
    api 'com.github.penemue:keap:0.3.0'
    testImplementation project(':entity-store').sourceSets.test.output
    testImplementation project(':utils').sourceSets.test.output
}