dependencies {
    compile project(':entity-store')
    compile 'com.github.penemue:keap:0.2.2'
    testCompile project(':entity-store').sourceSets.test.output
    testCompile project(':utils').sourceSets.test.output
}