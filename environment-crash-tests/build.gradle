dependencies {
    testImplementation project(':utils').sourceSets.test.output
    testImplementation project(':utils')
    testImplementation project(':environment')
}

test {
    testLogging.showStandardStreams = true
}